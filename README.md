# onboarding-automation
1. Make your own branch 
```
 git checkout -b <branch-name>
 git branch 
```  
2. Other important Git commands:
```
 git status
 git add .
 git commit -m "comment"
 git push 
```
3. Don't push to main branch unless authorized

        
